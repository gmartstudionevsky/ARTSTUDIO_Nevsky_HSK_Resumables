# Docs

This directory is reserved for project references and internal documentation.

## Quick start

1. Install dependencies: `npm i`
2. Run development server: `npm run dev`
3. Run quality checks:
   - `npm run lint`
   - `npm run typecheck`
   - `npm run build`
