# 2026-03-08 — Block 1.3 corrective wave (evidence-based closure)

## Context
External audit of the current repository state showed mismatch between the previous “Block 1.3 done” narrative and actual import compatibility behavior. The corrective objective for this wave is strict reconciliation of repository reality vs claimed status, with explicit before/after evidence.

## Why previous summary was insufficient
Previous summary asserted Block 1.3 completion, but did not isolate one critical semantic leak in import compatibility mapping:
- legacy header `Назначение` was accepted as alias for canonical `Статья затрат` in parser mapping,
- which distorts canonical model because `Назначение` belongs to legacy `Раздел` axis, not to financial axis (`Статья затрат`).

Result: import was not fully canon-first at mapping semantics level, therefore “done” was overstated.

## Baseline legacy findings before fix
Baseline was collected repo-wide before code changes with:
- `rg -n "Номенклатура|Назначение|Статья расходов|nomenclature|purpose|expense article"`
- focused searches under `src tests prisma docs`.

### Baseline counts (full repo)
- `Номенклатура`: 11
- `Назначение`: 16
- `Статья расходов`: 7
- `nomenclature`: 0
- `purpose`: 228 (internal domain/persistence naming + docs + tests)
- `expense article`: 0

### Baseline classification
- **primary user-facing:** no active primary copy leaks found in runtime UI components for `Номенклатура/Назначение/Статья расходов`.
- **import-facing (unacceptable semantic leak):**
  - `src/lib/import/xlsx/parse.ts` had mapping:
    - `'Статья затрат': ['Статья затрат', 'Статья расходов', 'Назначение']`
  - this incorrectly routed `Назначение` to cost article field.
- **tests/fixtures:** no explicit legacy-as-primary expectations detected for UI copy in touched test scope; import parser behavior lacked dedicated compatibility tests for this semantic split.
- **docs/historical:** multiple explicit mentions remain in roadmap/audit/glossary/spec docs as historical or compatibility context.
- **compatibility/internal only:** extensive `purpose*` identifiers remain internal in Prisma/domain/API contracts (allowed residual debt for this block).

## Files changed
### Import logic
- `src/lib/import/xlsx/parse.ts`

### Tests
- `tests/import.xlsx.parse.compatibility.test.ts` (new)

### Documentation
- `docs/changelog/2026-03-08-block-1.3-corrective.md` (this file)
- `docs/roadmap/progress-log.md`

## User-facing legacy removed
No additional runtime user-facing text replacements were required in this corrective wave; user-facing layer in touched scope was already canonical. Corrective focus was import semantics consistency.

## Import compatibility decisions
Implemented canon-first and model-safe header compatibility:
- Canonical primary headers remain primary:
  - `Позиция учёта`, `Раздел`, `Статья затрат`.
- Legacy aliases retained only where semantically valid:
  - `Номенклатура` -> `Позиция учёта` (compatibility-only)
  - `Назначение` -> `Раздел` (compatibility-only)
  - `Статья расходов` -> `Статья затрат` (compatibility-only)
- Invalid alias path removed:
  - `Назначение` is no longer alias for `Статья затрат`.

## Tests updated
Added explicit parser compatibility coverage:
1. legacy `Назначение` header maps to canonical `Раздел`.
2. legacy `Статья расходов` header maps to canonical `Статья затрат`.

This closes the previous blind spot where alias behavior was not directly asserted.

## Verification after fix
Post-fix searches repeated with the same query set:
- `rg -n "Номенклатура|Назначение|Статья расходов|nomenclature|purpose|expense article"`
- targeted `rg -n "Номенклатура|Назначение|Статья расходов" src tests prisma docs`

### Post-fix classification of remaining legacy
- **compatibility-only:**
  - `src/lib/import/xlsx/parse.ts`: legacy aliases preserved in header map with corrected semantics.
- **internal only:**
  - `purpose*` identifiers across Prisma/domain/app contracts.
- **historical docs:**
  - roadmap/audit/product canon/glossary documents describing legacy history and migration context.
- **unacceptable leak:** none found in primary runtime UI copy for target terms.

## Remaining legacy and why it is allowed
1. `purpose*` internals remain as migration-safe technical debt (DB/schema/API stability).
2. Legacy import aliases remain only as backward compatibility for old files and are now semantically aligned.
3. Historical documentation references remain as audit history and canon guardrails.

## Final status of Block 1.3
**Done (corrected and evidence-backed).**

Rationale:
- canonical language remains dominant in user-facing runtime layer,
- import mapping is now canon-first and semantically correct,
- dedicated compatibility tests added,
- remaining legacy localized to compatibility/internal/historical zones and documented.
